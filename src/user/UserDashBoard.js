import React from "react";
import Base from "../core/Base";

const UserDashBoard = () => {
    return (
        <Base title="User Dashboard Page">
            <h1>This is a user dashboard page</h1>
        </Base>
    )
};

export default UserDashBoard;
